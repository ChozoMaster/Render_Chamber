SUBDIRLIST(subdirectories ${CMAKE_CURRENT_SOURCE_DIR})
foreach(dir ${subdirectories})
	OPTION(BUILD_${dir} "Build application projects" OFF)
	IF(BUILD_${dir})
		add_subdirectory(${dir})
	ENDIF()
endforeach()
